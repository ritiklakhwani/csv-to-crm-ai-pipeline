# Where the Express API lives. In production this is the deployed Render URL.
# The value is inlined into the client bundle at build time, so it must be public.
NEXT_PUBLIC_API_URL=http://localhost:4000
